import React from 'react'
import { useFilterContext } from '../context/FilterContext';

function Filterfield() {
    const { handleFilterDel, handleFilterClr } = useFilterContext();
  return (
    <div></div>
  )
}

export default Filterfield